title: menozero's journal

author: menozero
# email:

twitter_username: menozero

# markdown: kramdown
kramdown:
  input: GFM
  hard_wrap: false

plugins:
  - jekyll-feed
  - jekyll-seo-tag
  - jekyll-sitemap

relative_links:
  enabled:     true
  collections: false

exclude:
  - Gemfile
  - Gemfile.lock

