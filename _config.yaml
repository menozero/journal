
title: menozero's journal

author: menozero
# email:

twitter_username: menozero

# theme:

# markdown: kramdown
kramdown:
  input: GFM
  hard_wrap: false

plugins:
  - jekyll-feed
  - jekyll-seo-tag
  - jekyll-sitemap

relative_links:
  enabled:     true
  collections: false

github:
  - metadata

exclude:
  - Gemfile
  - Gemfile.lock
  - vendor
